<?xml version="1.0" ?>
<windowex:WindowEx
    x:Class="书店管理系统.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cc="using:CoreServices.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:书店管理系统"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:windowex="using:WinUIEx"
    Width="1280"
    Height="720"
    IsResizable="False"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>
    <Grid x:Name="rootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <cc:CustomTitleBar
            x:Name="WindowTitleBar"
            Title="书店管理系统"
            SubTitle=""
            TargetWindow="{x:Bind}" />
        <cc:CustomNavigationView
            x:Name="MainNavigationView"
            Grid.Row="1"
            Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
            FooterPageItems="{x:Bind ViewModel.FooterPageItems, Mode=OneWay}"
            HeaderPageItems="{x:Bind ViewModel.HeaderPageItems, Mode=OneWay}"
            OpenPaneLength="192" />
    </Grid>
</windowex:WindowEx>
